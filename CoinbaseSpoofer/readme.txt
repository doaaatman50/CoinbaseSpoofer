By creating a proxy server we can intercept the requests sent between the browser and the coinbase server.
We can intercept the image verification process (front, back, selfe(Camera)) to send something else.

I used Titanium.Web.Proxy in this project to create a web proxy server.